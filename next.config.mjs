/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Reservado por si luego se necesitan Server Actions con payloads grandes (imágenes, adjuntos)
  },
};

export default nextConfig;
